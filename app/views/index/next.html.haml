#info
  %h1.depart
    = @bus.depart.strftime("%H:%M")
  .details
    %h3
      = kind_enum(@bus.kind)
      \-
      = @bus.note
      - if @bus.special
        %span.label.label-danger= t('helpers.overtime_car')
